---
import { Header, Button, Footer } from '@littlesticks/odyssey-theme-components';
import Layout from './Base.astro';
import type { Props as BaseHeadProps } from '../components/head/BaseHead.astro';
import Logo from '../components/Logo.astro';
import settings from '../config/settings';
import { footerLists, footerSocials } from '../config/footer';


export interface Props {
	seo?: BaseHeadProps;
}

import { nav } from '../config/nav.js';

const { seo } = Astro.props as Props;
---

<Layout seo={seo}>
	<Header rightMenu navData={nav} slot="header">
		<span slot="logo">
			<Logo />
		</span>
    <a class="whatsapp-btn" href="https://wa.me/5583982258089?text=Ol%C3%A1%20Lab%20%C3%93tica,%20gostaria%20de%20te%20fazer%20uma%20pergunta!" slot="action-item">WhatsApp</a>
		<!-- <Button class="whatsapp-btn" slot="action-item" href="https://wa.me/5583982258089?text=Ol%C3%A1%20Lab%20%C3%93tica,%20gostaria%20de%20te%20fazer%20uma%20pergunta!">WhatsApp</Button> -->
	</Header>
	<div id="page">
		<slot />
	</div>
	<Footer
		slot="footer"
		background="var(--theme-surface-1)"
		color="var(--theme-on-surface-1)"
		footerLists={footerLists}
    footerSocials={footerSocials}
		copyrightName={"Lab Ótica"}
    showLittleSticksPlug={false}
	>
		<Logo slot="logo" />
	</Footer>
</Layout>
